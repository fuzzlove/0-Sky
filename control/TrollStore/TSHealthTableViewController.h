#import <UIKit/UIKit.h>

/// Read-only Phase 2 device-health dashboard backed by the authenticated
/// loopback 0-Sky Core API. All transport work runs off the main thread.
@interface TSHealthTableViewController : UITableViewController
@end
